# Required: office-ui-fabric-react@7 declares React 16/17 as peer dep,
# which conflicts with React 18. This suppresses the conflict until
# migration to @fluentui/react@8+ is completed.
legacy-peer-deps=true
